{
  "name": "express-app",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "test": "jest --coverage",
    "build": "npx tsc",
    "dist": "npx tsc --project tsconfig.dist.json",
    "watch": "npx tsc --watch",
    "start": "nodemon src/index.mts",
    "clean": "rm -rf dist && rm -rf coverage"
  },
  "engines": {
    "node": "^18",
    "npm": "^9"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "redis": "^4.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^28.1.8",
    "@types/node": "^18.7.6",
    "jest": "^28.1.3",
    "nodemon": "^2.0.19",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
